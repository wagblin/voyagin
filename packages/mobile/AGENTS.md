# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Pinned to SDK 54 (not the latest) because the Expo Go app on the App Store only supports SDK 54 as of this writing — Expo Go rejects projects built against a newer SDK ("Project is incompatible with this version of Expo Go"). Check before bumping the `expo` package again.
